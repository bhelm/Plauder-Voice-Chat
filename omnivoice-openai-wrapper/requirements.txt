# OmniVoice OpenAI-compatible TTS server — GPU box only.
# Tested with Python 3.11 + CUDA. Pinned to the versions running in production;
# bump deliberately. Install into a DEDICATED venv (NOT the voice-chat .venv):
#   python3.11 -m venv omnivoice-env
#   omnivoice-env/bin/pip install -r requirements.txt
#
# torch/torchaudio MUST match your CUDA toolkit — install them FIRST from the
# right index, e.g.:
#   omnivoice-env/bin/pip install torch==2.12.1 torchaudio==2.11.0 \
#     --index-url https://download.pytorch.org/whl/cu124
# then the rest below.

omnivoice==0.1.5
transformers==5.12.1
huggingface-hub==1.21.0

fastapi==0.139.0
uvicorn==0.49.0
pydantic==2.13.4
python-multipart==0.0.20   # multipart form parsing for POST /v1/audio/voices

soundfile==0.14.0
pydub==0.25.1          # needs ffmpeg on PATH for mp3/opus/aac output
numpy==2.4.6
num2words==0.5.14      # German number/date normalisation (textnorm.py)
