# Voice-Chat — runtime dependencies (cloud default).
# The cloud path (STT/TTS via OpenAI, LLM via Fireworks) needs NO GPU/ML deps.
#
# Upper bounds are pinned so a major release can't silently break the
# SSE/transcription paths; bump them deliberately.

aiohttp>=3.9,<4
openai>=1.0,<2
numpy>=1.24,<3


# Tests
pytest>=7.0,<9

# --- Optional local GPU backends (install only when needed) ------------------
# STT_BACKEND=whisper_local:
#   pip install faster-whisper
# TTS_BACKEND=omnivoice_local:
#   pip install omnivoice torch   (see the k2-fsa/OmniVoice setup)
# SPEAKER_LOCK_ENABLED=1 (voice gate — only the enrolled owner voice is heard):
#   pip install sherpa-onnx   (bundles the kaldi-fbank the CAM++ model expects;
#   no torch needed) + a speaker-embedding model, see .env.example SPEAKER_MODEL_PATH.
# These packages are imported ONLY when the corresponding backend/feature is active.
