# Secrets — niemals committen
.env

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# Laufzeit-Artefakte
uploads/
house_data/
*.log
# Persistierte Hermes-Session-ID (rotiert bei "New session")
.hermes_session_id
.hermes_session_id.tmp

# Persisted active cloned-voice id (runtime state; see TTS_CLONE_ENABLED)
.active_voice
.active_voice.tmp

# Speaker lock: downloaded embedding models + the enrolled voice profile
models/
*.onnx
speaker_profile.json

# Backups / Editor / OS
*_orig.py
*.bak
.DS_Store
*.swp
# Optionales House-Mode-Artefakt (nicht im Repo)
speakers.json

# .env-Backups (enthalten Secrets)
.env.bak-*

# Interne Diagnose-Notizen (enthalten Session-Keys/Chat-IDs)
HERMES_MEMORY_NOTES.md
spk-dump/
